{
  "name": "npm",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "start": "npm run-script clean && npm run-script build && node server.js",
    "build": "mkdir -p dist && cp -r src/* dist",
    "clean": "rm -rf dist/*"
  },
  "dependencies": {
    "ejs": "^2.5.7",
    "express": "^4.16.2",
    "express-http-proxy": "^1.0.7",
    "http-proxy-middleware": "^0.17.4",
    "jquery": "^3.2.1",
    "moment": "^2.19.1",
    "node-datetime": "^2.0.3"
  },
  "main": "server.js",
  "author": "Roman",
  "license": "ISC"
}
